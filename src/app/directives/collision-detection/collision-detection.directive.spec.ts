import { CollisionDetectionDirective } from './collision-detection.directive';

describe('CollisionDetectionDirective', () => {
  it('should create an instance', () => {
    const directive = new CollisionDetectionDirective();
    expect(directive).toBeTruthy();
  });
});
