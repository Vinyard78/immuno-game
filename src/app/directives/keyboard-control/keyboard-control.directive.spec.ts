import { KeyboardControlDirective } from './keyboard-control.directive';

describe('KeyboardControlDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyboardControlDirective();
    expect(directive).toBeTruthy();
  });
});
